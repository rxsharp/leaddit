# leaddit - a reddit-based clone in Angular

## Build & development

### After cloning
 - npm install
 - bower install

Run `grunt` for building and `grunt serve` for preview.

## Testing

Running `grunt test` will run the unit tests with karma.
